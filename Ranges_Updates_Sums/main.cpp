#include <iostream>

struct state {
	long long val;
	long long lazy;
	long long settle;

};
int main() {
	std::ios_base::sync_with_stdio(false);
	std::cin.tie();
	std::cout.tie();

	int n, q;
	std::cin >> n >> q;
}
